<!-- markdownlint-disable MD013 -->

# Tagger

There's a great many Conventional Commit + SemVer bumpers out there but I'm currently on a project with some preordained rules about commit messages and tag formats so I needed one I could customize quite a bit.

For the config you can set, those customizations, see [.tagger.yaml](./.tagger.yaml).
